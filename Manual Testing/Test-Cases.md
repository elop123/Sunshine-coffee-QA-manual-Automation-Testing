# Sunshine Coffee - Test Case Suite

## Test Case 

| ID | Feature | Title | Preconditions | Steps | Expected Result | Status |
|---|---|---|---|---|---|---|
| **TC-01** | Product Browsing | Verify product catalog displays correct details and currency | User is on the homepage / product catalog page. | 1. Navigate to the product listing page.<br>2. Inspect product cards.<br>3. Verify name, image,roast level and price formatting. | All products display high-quality images, accurate titles, and prices formatted correctly in local currency (e.g., **400kr**). |
| **TC-02** | Product Browsing | Verify responsive layout across different screen viewports | Application is running across multiple device emulators. | 1. Open the catalog on Desktop, Tablet, and Mobile views.<br>2. Observe the product grid layout. | The layout scales fluidly. Elements, titles, and buttons do not overlap or break visual boundaries on mobile viewports. | ⬜ |
| **TC-03** | Product Browsing | Verify behavior when product catalog database is empty | Database has zero items active. | 1. Clear database inventory.<br>2. Navigate to the product listing page. | A user-friendly empty state message appears (e.g., "No products available") instead of a broken or blank page. |
| **TC-04** | Product Detail | Verify navigation to product detail page | User is on the product listing page with active items. | 1. Click on a specific coffee product card.<br>2. Observe the page shift and loaded content. | The dedicated product detail page loads within 3 seconds, showing matching description, pricing, and an enabled quantity selector. | 
| **TC-05** | Product Detail | Verify Add to Cart interaction increments the cart counter | User is on an individual product detail page. Header counter is at 0. | 1. Click the "Add to Cart" button.<br>2. Look at the navigation bar basket icon. | The header cart icon badge updates dynamically (increments by **1**)
| **TC-06** | Product Detail | Verify system handling of excessively large quantity input | User is on a product detail page with an editable quantity field. | 1. Input an excessively large number (e.g., **999**).<br>2. Click "Add to Cart". | The system safely blocks the action, throws an error message, or caps the total quantity automatically at maximum available stock. | 
| **TC-07** | Shopping Cart | Verify item removal from a multi-item cart  | User has added at least two distinct items to the shopping cart. | 1. Open the shopping cart view.<br>2. Click the "Remove" button next to one item. | The targeted item is deleted from the list, and the subtotal/grand total recalculates accurately in real-time. | 
| **TC-08** | Shopping Cart | Verify state transition when removing the final item | User has exactly one item inside the shopping cart. | 1. Open the shopping cart view.<br>2. Click the "Remove" button on that final item. | The item disappears, the cart balance drops to **0**, and a clean "Your cart is empty" graphic or message is displayed. | 
| **TC-09** | Shopping Cart | Verify cart quantity adjustments persist after browser refresh | User has an active item session inside the cart. | 1. Modify the item quantity counter from 1 to 3.<br>2. Hit **F5** to force a browser page refresh. | The updated quantity and recalculated total balance persist perfectly after the page reloads. | 
| **TC-10** | Checkout Flow | Verify blank contact field submit validation errors | User has items in the cart and proceeds to the checkout form page. | 1. Leave Email, Name, and Shipping address completely blank.<br>2. Click the "Submit Order" button. | Form submission is blocked. Targeted inline error alerts appear under each required empty input field. | 
